syntax = "proto3";

package protobuf;

option go_package = "./proto";

service StreamService {
  rpc GetRandomDataStream(Request) returns (stream Response) {}
}

message Request {
  int32 id = 1;
}

message Response {
  string result = 1;
}